import 'package:flutter/material.dart';

class InsurerWorkspacePage extends StatelessWidget {
  const InsurerWorkspacePage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Workspace Aseguradora/Broker (stub)')));
  }
}
