import 'package:flutter/material.dart';

class RenterWorkspacePage extends StatelessWidget {
  const RenterWorkspacePage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Workspace Arrendatario (stub)')));
  }
}
