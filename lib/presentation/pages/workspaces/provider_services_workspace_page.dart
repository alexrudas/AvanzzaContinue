import 'package:flutter/material.dart';

class ProviderServicesWorkspacePage extends StatelessWidget {
  const ProviderServicesWorkspacePage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Workspace Proveedor Servicios (stub)')));
  }
}
