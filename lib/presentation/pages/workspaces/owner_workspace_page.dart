import 'package:flutter/material.dart';

class OwnerWorkspacePage extends StatelessWidget {
  const OwnerWorkspacePage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Workspace Propietario (stub)')));
  }
}
