import 'package:flutter/material.dart';

class LegalWorkspacePage extends StatelessWidget {
  const LegalWorkspacePage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Workspace Legal (stub)')));
  }
}
